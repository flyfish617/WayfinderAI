# WayfinderAI 性能优化清单

> 优化目标：行程生成耗时从 1-2 分钟降至 30-60 秒，并让等待过程可见。
> 日期：2026-08-30

---

## 1. 改动清单

### 性能优化（本次核心）

| # | 文件 | 改动内容 |
|---|------|---------|
| 1 | `backend/app/config.py` | 新增 `LLM_ENABLE_THINKING: bool = False` 配置项 |
| 2 | `backend/.env` | 新增 `LLM_ENABLE_THINKING=False`（思考模式开关，可随时切回） |
| 3 | `backend/app/services/llm_service.py` | ① 初始化时计算 `_extra_body`（非 OpenAI 服务商附加 `enable_thinking` 参数）；② `think()` 流式调用附加该参数，并支持记录流式 usage；③ `invoke()` 同步调用附加该参数；④ 新增 `_current_usage_key` 供流式场景统计 token |
| 4 | `backend/app/services/redis_service.py` | ① 修复 `socket_timeout=5` 与 BLPOP 阻塞 5s 竞争导致的 `Timeout reading from socket`（提升至 10s）；② 新增通用缓存方法 `get_cached_json()` / `set_cached_json()` |
| 5 | `backend/app/agents/planner.py` | **核心改动**：① 新增 `_fetch_tool_data_direct()`——单次 MCP 连接顺序调用高德 `maps_text_search`（景点/酒店）与 `maps_weather`，结果写入 Redis 缓存（POI 6 小时 / 天气 3 小时）；② 新增 `_parse_tool_data()` / `_extract_amap_items()` / `_parse_amap_location()`——纯代码解析高德返回，替代原 LLM 结构化整理器；③ `plan_trip()` 删除 3 个专家 Agent 的 6 次 LLM 调用与 3 次 LLM 结构化整理；④ 最终编排 LLM 改为流式生成，配合节流回调实时上报进度 |
| 6 | `backend/app/agents/enhanced_agent.py` | `run()` 非工具分支支持 `stream_callback` 参数——传入后走流式 LLM 调用，每个 chunk 实时回调 |
| 7 | `backend/app/services/trip_service.py` | `progress_callback` 全链路穿透（`plan_trip` → `_build_and_store_plan` → `PlannerAgent.plan_trip`）；异步任务 worker 用回调实时更新 Redis 任务进度 |

**效果：一次行程规划的 LLM 调用从 10 次降至 1 次；工具查询结果按城市缓存，重复请求近零开销。**

### 此前的修复（同一会话，一并记录）

| 文件 | 问题 | 修复 |
|------|------|------|
| `backend/requirements.txt` | `helloagents` 包名错误，pip 无法安装 | 改为 `hello-agents` |
| `backend/run.py` + `backend/app/services/vector_memory_service.py` | `HF_ENDPOINT` 在 huggingface_hub 导入后才设置，镜像不生效，模型下载直连官方源超时 | 环境变量提前到所有重型库导入之前 |
| `backend/app/agents/planner.py` | 缺 `Dict`/`Any` 导入，Python 3.13 下 NameError | 补齐 typing 导入（3.14 惰性注解掩盖了此问题） |
| `frontend/src/views/Result.vue` | 源文件曾被错误编码保存（UTF-8 按 GBK 误读），66 处模板文字乱码 | 脚本自动还原 24 处 + 手工映射 42 处，复查零残留 |

---

## 2. 原调用链路是什么？为什么需要绕过专家 LLM 调工具

### 原链路（共 10 次 LLM 调用）

```
plan_trip()
│
├─ 阶段1：三个专家 Agent 并行（ThreadPoolExecutor，各含 2 次 LLM）
│   ├─ 景点Agent：LLM① 决定调工具 → MCP maps_text_search（~1-2s）→ LLM② 总结返回结果
│   ├─ 酒店Agent：LLM③ → MCP → LLM④
│   └─ 天气Agent：LLM⑤ → MCP maps_weather → LLM⑥
│
├─ 阶段2：结构化整理（3 次 LLM）
│   └─ _synthesize_agent_output() 把每个专家的"总结文本"再让 LLM 转回 JSON（⑦⑧⑨）
│
└─ 阶段3：总规划 Agent（LLM⑩）
    └─ 一次性生成完整多日行程 JSON（3000-5000 token，最大耗时头）
```

### 为什么绕过专家 LLM

1. **工具参数早已写死，LLM 是"复读机"**。原提示词形如：
   `[TOOL_CALL:amap_maps_text_search:keywords=美食,city=成都]`
   ——调用哪个工具、什么参数在代码里已完全确定，发给 LLM 只是让它原样"决定"一遍，信息增益为零。
2. **高德返回的本身就是结构化 JSON**。原链路却让 LLM 把 JSON 总结成文本，再让另一个 LLM 把文本整理回 JSON——两次无意义的转换，还可能丢字段。
3. **时间成本极端不成比例**：专家阶段 6 次 LLM + 3 次整理 = 约 30-60 秒；工具直连只需 2-4 秒（MCP 子进程冷启动为主）。
4. **稳定性反而提升**：LLM 转发参数可能拼错、漏调、幻觉字段；确定性代码路径不存在这些问题。降级逻辑（工具失败 → 通用文案）原样保留。

代价：专家 Agent 的"多智能体协作"变成了编排器 + 工具层（协作消息流不再参与数据链路）。若课程展示需要协作叙事，可保留通信框架，仅数据路径走直连。

---

## 3. 为什么关闭思考模式？不关有什么优点

### 为什么关

- Qwen3.5 是**混合推理模型**：思考模式下模型会先生成数百到数千 token 的内部推理，再输出正式答案。这些 token 计费、计时间，但对"按固定 schema 填 JSON"的任务帮助极小。
- 行程编排的正确性主要由代码层保障：提示词给了完整 JSON schema + 11 条生成规则，后端 `_validate_and_filter_plan` 还会做地理过滤和预算校验兜底。
- 实测：思考输出可能让总 token 翻 2-5 倍，直接拉长最耗时的阶段 3。

### 不关的优点（即 `LLM_ENABLE_THINKING=True` 的价值）

1. **复杂约束权衡更强**：当预算/时间/地理/偏好相互冲突时（如"低预算但想住地铁附近"），思考链能做出更合理的取舍。
2. **格式自纠错**：模型可在思考中发现自己的 JSON 结构问题并修正，降低解析失败率（虽然我们后端有校验兜底）。
3. **创意质量**：每日主题的故事性、景点组合的新颖度可能更好。
4. **长链路推理任务**（未来做逐天并行时的"骨架分配"步骤）思考模式更有价值。

**建议**：默认关闭求速度；若发现行程质量可感知下降或 JSON 解析失败率上升，把 `.env` 的 `LLM_ENABLE_THINKING=True` 对比一次。

---

## 4. 流式输出只能在终端看见，展示层仍然看不见——现状与原因

### 现状（用户观察正确）

- 流式 chunk 确实只打印在**后端终端**（`llm_service.think()` 里的 `print(content, end="")`）。
- 前端加载弹窗只能看到节流后的**字符计数**（`AI generating: 1200 chars`），看不到实际生成的内容。

### 原因：架构通道缺失

当前是"异步任务 + 1.5s 轮询"架构：

```
LLM 流式 chunk → 节流回调 → Redis 任务 hash 的 message 字符串 → 前端轮询 TripTaskResponse
```

- Redis 里只存了进度百分比和一句 message，**没有存部分生成内容的字段**；
- 前端轮询接口 `GET /tasks/{id}` 返回的是任务状态摘要，不是内容流；
- 所以"流式"目前只优化了后端可观测性（终端 + 进度百分比），前端用户看到的仍是黑盒 + 计数。

### 要让展示层看到流式内容的三个可选方案

| 方案 | 做法 | 改动量 | 实时性 |
|------|------|--------|--------|
| A. 轮询带预览（轻量） | 节流回调里把 `partial_text` 的尾部 200 字也写进任务 hash（新增 `partial_content` 字段），`TripTaskResponse` 加字段，前端弹窗展示"生成预览…" | 小（后端 2 处 + 前端 1 处） | 1.5s 粒度 |
| B. SSE 推流（标准） | 新增 `GET /api/v1/trips/tasks/{id}/stream` 端点，用 `EventSourceResponse` 推送进度与内容片段；前端用 `EventSource` 替代轮询 | 中（新端点 + 前端改造） | 秒级 |
| C. 全链路流式（彻底） | 放弃异步任务模式，同步接口改 `StreamingResponse`，生成多少推多少，前端边收边渲染 | 大（API 形态重构、断线处理、与现有任务队列并存策略） | 实时 |

当前已落地的流式基建（stream_callback、节流、usage 统计）是方案 A/B 的直接前置，选任一方案都可以复用，无需重做。

---

## 附：优化前后对比

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 单次规划 LLM 调用 | 10 次 | 1 次（仅最终编排） |
| 数据获取 | 30-60s（LLM 包裹） | 2-4s 直连，缓存命中 <0.1s |
| 等待可见性 | 黑盒动画 | 阶段进度（25→30→45→55→69→70→90→100）+ 生成字数 |
| 空闲日志 | 每轮询一次刷一条 Redis 超时 ERROR | 无 |
| 质量保障 | 依赖 LLM 自觉 | schema 校验 + 地理/预算代码校验兜底（不变） |
